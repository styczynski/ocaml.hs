let (>>) x y = fun c -> x (y c) in
let compose x y = fun c -> (x (y c)) in
let (+) x y = value_add x y in
let (*) x y = value_mul x y in

let f x = x+1 in
let g x = x*2 in

let u = (compose f g) in u 2
;;
